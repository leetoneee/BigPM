import React from 'react'

type Props = {
  id: string;
  setIsModalNewTaskOpen: (isOpen: boolean) => void;
};

const Overview = ({ id, setIsModalNewTaskOpen }: Props) => {
  return (
    <div>Overview</div>
  )
}

export default Overview